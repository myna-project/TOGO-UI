import { Measure } from './measure';

export class Measures {
  drain_id: number;
  drain_name: string;
  unit: string;
  type: string;
  measure_type: string;
  decimals: number;
  measures: Measure[];
}
