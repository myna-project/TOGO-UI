import { DrainControlDetail } from './draincontroldetail';

export class DrainControl {
  id: number;
  name: string;
  org_id: number;
  type: string;
  cron_second: string;
  every_second: number;
  starting_second: number;
  selected_seconds: number[];
  from_second: number;
  to_second: number;
  cron_minute: string;
  every_minute: number;
  starting_minute: number;
  selected_minutes: number[];
  from_minute: number;
  to_minute: number;
  cron_hour: string;
  every_hour: number;
  starting_hour: number;
  selected_hours: number[];
  from_hour: number;
  to_hour: number;
  cron_day: string;
  cron_day_month: string;
  every_day_month: number;
  starting_day_month: number;
  selected_days_week: string[];
  cron_day_week: string;
  every_day_week: number;
  starting_day_week: string;
  selected_days_month: number[];
  last_weekday: string;
  days_before: number;
  nearest_day: number;
  day_counter: string;
  weekday_counter: string;
  cron_month: string;
  every_month: number;
  starting_month: number;
  selected_months: string[];
  from_month: number;
  to_month: number;
  mail_receivers: string;
  errors: number;
  last_mail_sent_time: string;
  details: DrainControlDetail[];
}
